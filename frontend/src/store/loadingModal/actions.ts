import { ActionTree } from "vuex";
import { LoadingModalState } from "./types";
import { RootState } from "../types";

export const actions: ActionTree<LoadingModalState, RootState> = {
  // fetchData({ commit }): any {
  //   commit;
  // }
};
