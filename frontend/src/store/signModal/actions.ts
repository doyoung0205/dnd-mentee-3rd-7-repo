import { ActionTree } from "vuex";
import { SignModalState } from "./types";
import { RootState } from "../types";

export const actions: ActionTree<SignModalState, RootState> = {
  // fetchData({ commit }): any {
  //   commit;
  // }
};
